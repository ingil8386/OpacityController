<Window x:Class="WindowOpacityController.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window Controller"
        Height="500"
        Width="760"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#111827"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Height" Value="30"/>
        </Style>

    </Window.Resources>

    <Grid Background="Transparent"
          Margin="12">

        <Border Background="#F9FAFB"
                CornerRadius="18"
                MouseLeftButtonDown="Window_MouseLeftButtonDown">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 상단 영역 -->
                <Border Grid.Row="0"
                        Background="White"
                        CornerRadius="18,18,0,0">

                    <Grid Margin="24,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Window Controller"
                                       FontSize="22"
                                       FontWeight="Bold"/>

                            <TextBlock Text="창 투명도 조절 및 숨기기 도구"
                                       FontSize="13"
                                       Foreground="#6B7280"
                                       Margin="0,4,0,0"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="×"
                                Style="{StaticResource CloseButton}"
                                Click="CloseButton_Click"
                                VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- 본문 -->
                <Grid Grid.Row="1"
                      Margin="24">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 대상 창 선택 -->
                    <Border Grid.Row="0"
                            Background="White"
                            CornerRadius="14"
                            Padding="18"
                            Margin="0,0,0,14">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="대상 창 선택"
                                       FontSize="15"
                                       FontWeight="Bold"/>

                            <TextBlock Grid.Row="1"
                                       Text="투명도 또는 숨김 기능을 적용할 창을 선택하세요."
                                       FontSize="13"
                                       Foreground="#6B7280"
                                       Margin="0,5,0,12"/>

                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="130"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox x:Name="WindowComboBox"
                                          DisplayMemberPath="DisplayName"
                                          Margin="0,0,12,0"/>

                                <Button Grid.Column="1"
                                        Content="새로고침"
                                        Style="{StaticResource PrimaryButton}"
                                        Click="RefreshButton_Click"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- 투명도 조절 -->
                    <Border Grid.Row="1"
                            Background="White"
                            CornerRadius="14"
                            Padding="18"
                            Margin="0,0,0,14">

                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="투명도 조절"
                                               FontSize="15"
                                               FontWeight="Bold"/>

                                    <TextBlock Text="값이 낮을수록 더 투명해집니다."
                                               FontSize="13"
                                               Foreground="#6B7280"
                                               Margin="0,5,0,0"/>
                                </StackPanel>

                                <Border Grid.Column="1"
                                        Background="#EFF6FF"
                                        CornerRadius="8"
                                        Padding="12,7"
                                        VerticalAlignment="Center">

                                    <TextBlock x:Name="OpacityValueText"
                                               Text="현재 값: 255"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="#2563EB"/>
                                </Border>
                            </Grid>

                            <Slider x:Name="OpacitySlider"
                                    Minimum="1"
                                    Maximum="255"
                                    Value="255"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="False"
                                    ValueChanged="OpacitySlider_ValueChanged"/>

                            <Grid Margin="0,4,0,0">
                                <TextBlock Text="투명"
                                           HorizontalAlignment="Left"
                                           FontSize="12"
                                           Foreground="#9CA3AF"/>

                                <TextBlock Text="불투명"
                                           HorizontalAlignment="Right"
                                           FontSize="12"
                                           Foreground="#9CA3AF"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 단축키 -->
                    <Border Grid.Row="2"
                            Background="White"
                            CornerRadius="14"
                            Padding="18"
                            Margin="0,0,0,14">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0"
                                        Orientation="Horizontal">

                                <Border Background="#EEF2FF"
                                        CornerRadius="8"
                                        Padding="10,6"
                                        Margin="0,0,10,0">

                                    <TextBlock Text="Ctrl + ]"
                                               FontSize="13"
                                               FontWeight="Bold"
                                               Foreground="#4F46E5"/>
                                </Border>

                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="선택 창 숨기기"
                                               FontSize="13"
                                               FontWeight="SemiBold"/>

                                    <TextBlock Text="다시 누르면 보이기"
                                               FontSize="12"
                                               Foreground="#6B7280"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1"
                                        Orientation="Horizontal">

                                <Border Background="#ECFDF5"
                                        CornerRadius="8"
                                        Padding="10,6"
                                        Margin="0,0,10,0">

                                    <TextBlock Text="Ctrl + ["
                                               FontSize="13"
                                               FontWeight="Bold"
                                               Foreground="#059669"/>
                                </Border>

                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="이 프로그램 숨기기"
                                               FontSize="13"
                                               FontWeight="SemiBold"/>

                                    <TextBlock Text="다시 누르면 보이기"
                                               FontSize="12"
                                               Foreground="#6B7280"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- 상태 메시지 -->
                    <Border Grid.Row="3"
                            Background="White"
                            CornerRadius="14"
                            Padding="16">

                        <TextBlock x:Name="ResultText"
                                   Text="준비 완료"
                                   FontSize="13"
                                   Foreground="#374151"
                                   TextWrapping="Wrap"/>
                    </Border>

                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>